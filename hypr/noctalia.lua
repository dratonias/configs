-- Generated by Noctalia

local primary = "rgb(8cff00)"
local surface = "rgb(32302f)"
local on_surface = "rgb(b8e6ff)"
local secondary = "rgb(3fd9ff)"
local on_secondary = "rgb(1d2021)"
local error = "rgb(e03636)"
local on_error = "rgb(1d2021)"
local shadow = "rgb(282828)"

local function apply_theme()
    hl.config({
        general = {
            col = {
                active_border = on_surface,
                inactive_border = surface,
            },
        },
        decoration = {
            shadow = {
                color = shadow,
            },
            glow = {
                color = shadow,
            },
        },
        group = {
            col = {
                border_active = secondary,
                border_inactive = surface,
                border_locked_active = error,
                border_locked_inactive = surface,
            },

            groupbar = {
                gradients = true,
                col = {
                    active = secondary,
                    inactive = surface,
                    locked_active = error,
                    locked_inactive = surface,
                },
                text_color = on_secondary,
                text_color_inactive = on_surface,
                text_color_locked_active = on_error,
                text_color_locked_inactive = on_surface,
            },
        },
    })
end

return {
    colors = {
        primary = primary,
        surface = surface,
        on_surface = on_surface,
        secondary = secondary,
        on_secondary = on_secondary,
        error = error,
        on_error = on_error,
        shadow = shadow,
    },
    apply_theme = apply_theme
}
